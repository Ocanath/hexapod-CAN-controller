function m = fk_sinpoly(hb_0, q, links)
    assert(isa(hb_0,'int32')*
    assert(size(q,2)==size(links,2), 'Error: num dofs must equal num links');
    n = size(q,2);
    m = hb_0;
    for i = 1:n
%       m = m*Hz_sinpoly(q(i))*links{i};  
      m = 
      m = bitshift(m,-12);  %remove order 12 from mult.
    end
end