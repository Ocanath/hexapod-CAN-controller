function m = dh_to_mat4(d,a,alpha)
    nd = size(d,2);
    na = size(a,2);
    na


    m = [
            1 0 0 a;
            0 cos(alpha) -sin(alpha) 0;
            0 sin(alpha) cos(alpha) d;
            0 0 0 1;
        ];
end